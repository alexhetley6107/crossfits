import { Canvas, Meta } from '@storybook/blocks';
import { Example } from './Example.tsx';
import * as Avatar from './Avatar.stories.tsx';

<Meta of={Avatar} title="Avatar" />

# Button

<Canvas of={Avatar.Base} />

## Usage

```ts
import { Avatar } from 'remindee-ui-kit';

export const Example = () => {
  return <Avatar />;
};
```

### Arguments

- **className** - A string that sets css class to the root of component.
- **text** - A string that shows text inside Avatar.
